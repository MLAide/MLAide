language: node_js
node_js:
  - "12.18.3"
dist: trusty
sudo: required
branches:
  only:
    - master
before_script:
  - npm install -g @angular/cli
script:
  - cd web-ui
  - npm run test-headless
  - ng build
